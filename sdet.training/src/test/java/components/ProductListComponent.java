package components;

import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;

public class ProductListComponent extends ComponentSuperClass {

	public ProductListComponent(SearchContext searchContext, WebDriver driver) {
		super(searchContext, driver);
	}
}
